version: '3'

services:
  gateway:
    image: nginx:alpine
    container_name: docker-gateway
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
